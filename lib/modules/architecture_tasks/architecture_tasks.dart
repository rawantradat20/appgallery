import 'package:flutter/material.dart';
class architecturetasks extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('architecture TASKS',
        style: TextStyle(
          fontSize: 25,
          fontWeight: FontWeight.bold,
        ),),
    );
  }
}

