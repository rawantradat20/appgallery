import 'package:flutter/material.dart';
class newtasks extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('NWE TASKS',
      style: TextStyle(
        fontSize: 25,
        fontWeight: FontWeight.bold,
      ),),
    );
  }
}
